# TODO (Human)

> Yes, TODO items for humans.
>
> Items that need a person — writing prose, making product decisions, running
> things on real accounts, talking to other humans. Tracked here so they don't
> clutter the code-side checklist in [`TODO.md`](./TODO.md).

Legend: `[ ]` not started · `[~]` in progress · `[x]` done · `[!]` blocked

---

## Prose & content

Public-facing writing. Voice matters; better drafted by a person and refined
rather than generated.

### `README.md` (final)

- [ ] Replace the current scaffolded README with a launch-ready version covering:
  - Overview / pitch
  - Install (`npm i -D ovellum` / `npx ovellum init`)
  - Quickstart (3-step path from `init` to a built `docs/`)
  - Mode comparison (hybrid / manual / auto) with when to use each
  - Config reference (link to `docs/config.md` once written)
  - Tagging guide (link to `docs/tagging.md` once written)
  - Badges: CI status, npm version, license, coverage

### `CONTRIBUTING.md` (final)

- [ ] Expand the scaffolded CONTRIBUTING.md with:
  - Dev setup (clone, `pnpm install`, `pnpm -w run demo`)
  - How to run tests (`pnpm exec turbo run test --filter='@ovellum/*'`)
  - PR process: branch naming, commit cadence, review expectations
  - Release process (changesets workflow)
  - Code of conduct pointer

### Self-hosted `docs/` (Ovellum documenting itself, hybrid mode)

- [ ] `docs/getting-started.md` (manual, hand-written)
- [ ] `docs/config.md` (manual)
- [ ] `docs/tagging.md` (manual — the tagging spec, prose form)
- [ ] `docs/orphans.md` (manual — how to handle orphaned sections)
- [ ] `docs/contributing.md` (manual)

> Note: `docs/api/` will be auto-generated by `ovellum build` from
> `packages/*/src/`. That's a code task, tracked in [`TODO.md`](./TODO.md)
> Phase 8.

---

## Product decisions

Each of these needs a human call before the code change can land. Capture the
decision plus the why so the implementer doesn't have to re-litigate it.

- [ ] **Should `ovellum clean` touch `.ovellum/orphans/`?**
      Lean: no, not without an explicit flag — orphans are committed-to-repo
      manual writing, deleting them by default is footgun-y.

- [ ] **Should watch mode reload config on `ovellum.config.*` change, or require a restart?**
      Lean: restart with a clear message ("config changed, restart `ovellum watch`"). Config
      reload mid-watch is a small ergonomic win but a large source of subtle bugs.

- [ ] **What does `@preserve` on a class JSDoc mean — protect the whole class section,
      or just the class-level comment?**
      Lean: just the class-level comment block. Individual method sections stay
      independent so a single hand-written class note doesn't freeze every method's docs.

- [ ] **Should generated output files be committed to version control, or
      gitignored?**
      Lean: committed by default (so PRs show doc diffs and reviewers can sanity-check), but
      make it configurable via `ovellum.config.ts` for projects that prefer otherwise.

- [ ] **Pure black / pure white in themes — keep banned or allow opt-in?**
      Currently `STYLES.md` says use `--color-zinc-950` / `--color-zinc-50`. Decide whether
      themes like Solarized that have literal `#000` equivalents need an escape hatch.

---

## Manual end-to-end tests

Things that exercise real terminals / real npm / real network. Cannot be done
inside CI without significant setup; cheaper to do by hand before each release.

- [ ] **`npx ovellum init` on a fresh TypeScript project** — verify the prompts
      flow, the generated `ovellum.config.ts` makes sense, and the first
      `ovellum build` produces sensible output.
- [ ] **`npx ovellum build` in `auto`, `manual`, and `hybrid` modes on the `examples/` projects** —
      confirm exit codes + summary text on each mode.
- [ ] **`npm pack` smoke test** — extract the tarball, install it in a scratch
      project, verify `bin.ovellum` resolves and runs.

---

## Release & launch

Account/credential operations and one-shot launch tasks. Each is a single
event with a clear "done" state.

- [ ] Write GitHub release notes for `v0.1.0` (changelog + highlights).
- [ ] Publish to npm as `0.1.0` under the `next` pre-release tag.
- [ ] Move the `next` tag to `latest` once the release is stable.
- [ ] Announce in relevant communities. Open-source launch strategy TBD —
      candidate venues: TypeScript subreddit, HN Show, Bluesky/Mastodon dev
      circles, the unjs community Discord.

---

## Repo housekeeping

- [ ] Decide on the public site URL. README currently advertises
      `https://ovellum.oss.oinam.com` — confirm that's the intended destination
      and that DNS / hosting is reserved.
- [ ] Confirm GitHub org/owner (`oinam` per README) and that the repo URL is
      stable before publicizing.
