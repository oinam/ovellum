# Ovellum - Features (current state)

What works **right now**, at this commit. Updated in the same commit as any
change that adds or modifies a user-visible feature.

For the original design vision see [`DESIGN.md`](./DESIGN.md). For roadmap and
deferred items see [`TODO.md`](./TODO.md). For human-only tasks (writing prose,
product decisions, release) see [`TODO.md`](../../TODO.md). For
terminology see the [glossary](https://ovellum.oss.oinam.com/docs/reference/glossary/).

Last updated: 2026-06-26 (0.15.0 shipped; D2 programmatic API + D5 deploy recipes + AI-Native MCP M1–M3: resources/prompts, Claude Code plugin, search_docs/reattach)

Status legend:

- `done` — shipped and exercised end-to-end
- `partial` — a v0 slice exists; named gaps live in `TODO.md`
- `deferred` — designed but not yet built (tracked in `TODO.md`)

---

## 1. Modes

Ovellum runs in one of three modes, set via `mode:` in `ovellum.config.{json,ts,js}`.

| Mode     | Status | What it does                                                                                                                                         |
| -------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| `hybrid` | done   | Default. Auto-generates Markdown from TS/JS source, then merges existing `<!-- @manual:start -->` blocks back in. Orphans go to `.ovellum/orphans/`. |
| `manual` | done   | Pure static-site builder from `.md` files → HTML. No source parsing. See §4 ([`@ovellum/site`](#4-site-builder---ovellumsite)).                      |
| `auto`   | done   | Auto-generate Markdown only. Existing output is overwritten. No merge step.                                                                          |

---

## 2. Core (`@ovellum/core`)

Shared types, config schema, error class. Consumed by every other package.

| Feature                                                                | Status | Notes                                                                                          |
| ---------------------------------------------------------------------- | ------ | ---------------------------------------------------------------------------------------------- |
| IR types (`DocNode`, `DocFile`, `DocProject`, `DocParam`, `DocReturn`) | done   | Per [`DESIGN.md` §6](./DESIGN.md#6-intermediate-representation-ir)                             |
| `OvellumConfig` schema                                                 | done   | Full reference in the [config reference](https://ovellum.oss.oinam.com/docs/reference/config/). Includes `site`, `protect`, format, mode, paths. |
| `defineConfig()` helper                                                | done   | Identity function for type-safe `ovellum.config.ts`.                                           |
| `loadOvellumConfig({ cwd, configFile })`                               | done   | Loads via `c12`; applies defaults; validates. Returns `{ config, configFile, cwd }`.           |
| `loadDirectoryOverride(rootCwd, targetDir, root)`                      | done   | Walks from root → target, merges every nested `ovellum.config.*` (deepest wins).               |
| `parseFrontmatterOverride(frontmatter)`                                | done   | Extracts `ovellum:` block from `.md` frontmatter for per-file mode overrides.                  |
| `mergeConfig(base, override)`                                          | done   | Shallow merge; arrays replaced wholesale; `protect` and `site` merged field-by-field.          |
| `validateUserConfig(input)`                                            | done   | Hand-rolled validator; throws `ConfigError` with `code` + `hint`.                              |
| `OvellumError` / `ConfigError`                                         | done   | Typed error base + config-specific subclass.                                                   |

**Tests:** 29 vitest cases — defaults, full config, every validation error, merge rules, frontmatter overrides, per-directory loading.

---

## 3. Pipeline (auto + hybrid modes)

### 3.1 Parser (`@ovellum/parser`)

| Symbol kind             | Status   | Notes                                                                      |
| ----------------------- | -------- | -------------------------------------------------------------------------- |
| `function`              | done     | Generics + JSDoc + params + return type. Overloads deferred.               |
| `class`                 | partial  | Methods + properties + extends + implements. Constructor section deferred. |
| `interface`             | done     | Properties + methods + extends.                                            |
| `type` alias            | done     | Name + RHS + generics.                                                     |
| `enum`                  | done     | Members with optional initializer values.                                  |
| `const` / `let` / `var` | deferred | Deferred.                                                                  |
| Module-level `@module`  | partial  | Only when attached to the first statement of the file.                     |

| JSDoc tag                        | Status                                               |
| -------------------------------- | ---------------------------------------------------- |
| `@param`, `@returns` / `@return` | done                                                 |
| `@throws` / `@exception`         | done                                                 |
| `@example`                       | done                                                 |
| `@deprecated`                    | done                                                 |
| `@since`, `@see`                 | partial (extracted; rendering in generator deferred) |
| `@remarks`, `@description`       | done                                                 |
| `@preserve` flag                 | partial (flag on `DocNode`; auto-wrapping deferred)  |
| `@internal` flag                 | done                                                 |
| Unknown tags                     | done (collected into the `tags` bag)                 |

| Edge case                      | Status   |
| ------------------------------ | -------- |
| Re-exports / barrel files      | deferred |
| Circular imports               | deferred |
| Overloaded functions           | deferred |
| Namespace exports              | deferred |
| `declare module` augmentations | deferred |

Anchor IDs: done `{relativeFilePath}::{symbolPath}` per [`DESIGN.md` §8.3](./DESIGN.md#83-anchor-ids-and-stability).

**Tests:** 12 vitest cases — symbol types and filtering, plus complex-type extraction (generics, optional/default/rest params, union/intersection types, interface heritage, generic union type aliases, the `includePrivate` gate).

### 3.2 Generator (`@ovellum/generator`)

| Feature                                                                    | Status   | Notes                                              |
| -------------------------------------------------------------------------- | -------- | -------------------------------------------------- |
| `generateDocs(project, config)` → `Map<outputPath, markdown>`              | done     |                                                    |
| Output path mapping `src/foo.ts` → `docs/foo.md`                           | done     | `.mdx` extension wired but JSX detection deferred. |
| Frontmatter (`title`, `source`, `generated`, `ovellum: true`)              | done     |                                                    |
| Function template (signature + params table + returns + throws + examples) | done     |                                                    |
| Class template (heritage + methods table + properties table)               | partial  | Constructor section deferred.                      |
| Interface template (members table)                                         | done     |                                                    |
| Type alias template                                                        | done     |                                                    |
| Enum template (members with values)                                        | done     |                                                    |
| Variable / const template                                                  | deferred |                                                    |
| Anchor comments `<!-- ovellum:anchor id="…" generated="…" -->`             | done     | On every top-level + child node.                   |
| `@deprecated` callout                                                      | partial  | Plain blockquote. Could wrap in `> [!WARNING]` for the rendered site to pick up the styled callout. |
| `@since` / `@see` rendering                                                | deferred |                                                    |
| Sidebar / `_index.md` generator                                            | deferred |                                                    |
| MDX mode (JSX-in-`@example` detection)                                     | deferred |                                                    |

**Tests:** 7 vitest cases (function rendering, mdx path mapping, multi-file, plus node-kind rendering: deprecated/since banners, class Properties/Methods grouping, enum members).

### 3.3 Reader (`@ovellum/reader`)

| Feature                                                              | Status   | Notes                                                                                     |
| -------------------------------------------------------------------- | -------- | ----------------------------------------------------------------------------------------- |
| `readManualDoc(path)` / `parseManualDoc(raw, path)`                  | done     |                                                                                           |
| Frontmatter via `gray-matter`                                        | done     |                                                                                           |
| Protected zone extraction (`<!-- @manual:start id="…" -->` / `:end`) | done     | Regex-based; positional fallback IDs when `id` omitted.                                   |
| Anchor association (block → nearest preceding `ovellum:anchor`)      | done     |                                                                                           |
| Error: unclosed / nested / stray `@manual:end`                       | done     | `OvellumError` with codes `UNCLOSED_MANUAL_TAG`, `NESTED_MANUAL_TAG`, `STRAY_MANUAL_END`. |
| Positional-fallback warning                                          | done     | Reader emits one warning per zone that falls back to `manual-block-N`; surfaced through the CLI build summary. |
| Validation mode (link checker, required frontmatter)                 | deferred | Needs `remark` stack.                                                                     |

**Tests:** 9 vitest cases.

### 3.4 Merger (`@ovellum/merger`)

| Feature                                                                     | Status   | Notes                                               |
| --------------------------------------------------------------------------- | -------- | --------------------------------------------------- |
| `merge(generated, manual, opts?)` → `{ content, orphans, warnings }`        | done     |                                                     |
| Section detection (anchor → next heading boundary)                          | done     | Splices manual blocks at section end.               |
| Orphan quarantine                                                           | done     | Writes `.ovellum/orphans/{YYYY-MM-DD}_{slug}.md`.   |
| `OrphanRecord` metadata (orphaned, source_file, anchor_id, manual_block_id) | done     |                                                     |
| Anchor last-seen timestamp on orphans                                       | deferred | IR now persisted (A1, `.ovellum/ir.json`); last-seen wiring is A4. |
| `@preserve` auto-wrapping in generator                                      | done     | **A5 (2026-06-25).** Hybrid build wraps a `@preserve` symbol's generated body in a `@manual` zone keyed by the node id (`templates.ts` `wrapPreserved`, gated `config.mode === 'hybrid'` in `generate.ts`; top-level nodes + method members; properties/table excluded). Merger strips generated-side seed zones when splicing the author's copy (`merge.ts` `stripGeneratedBlocks`) so there's no duplication. First build seeds; edits survive; deletion orphans. Pinned by generator `preserve.test.ts` (3) + cli `preserve.test.ts` (3, full cycle) + merge-survival unaffected. |

**Tests:** 8 vitest cases.

### 3.5 CLI (`ovellum`)

See the [CLI reference](https://ovellum.oss.oinam.com/docs/reference/cli/) for full reference.

| Subcommand        | Status   |
| ----------------- | -------- |
| `ovellum init`    | done     | Scaffolds `ovellum.config.ts` + starter `index.md` + `.gitignore`. **Hybrid starter shows the moat (U3, 2026-06-27):** in hybrid mode the starter `index.md` carries a `@manual id="welcome-note"` protected-zone example ("Add your own notes here — this block survives every rebuild"); manual mode unchanged. `renderStarterIndex` exported + pinned by `init-render.test.ts` (2). **Agent packaging (C4, 2026-06-24):** also writes a mode-aware **`AGENTS.md`** (`renderAgentsMd` — hybrid/auto lead with the protected-zone contract, manual leads with "edit content, never output"; only-if-absent). Fixed a stale scaffold marker (`ovellum:manual:start` → `@manual:start`). **Claude Skill** at `plugins/ovellum/skills/ovellum-docs/SKILL.md` (bundled in the Claude Code plugin — see M2). Pinned by `agents-md.test.ts` (4) + init smoke. |
| `ovellum build`   | done     | manual / hybrid / auto. **Deploy-anywhere flags (D1/D4, 2026-06-14):** `--out <dir>` overrides `config.output` and `--base <path>` overrides `site.basePath` per-invocation (`applyOverrides` in `run-build.ts`) so a CI/deploy pipeline points the build at any folder (e.g. a repo's `/docs`) without editing config. `--manifest` writes `<output>/.ovellum/manifest.json` — a hashed inventory (path/bytes/sha256, sorted, OS-junk + own-dir excluded) for atomic/incremental deploys (`dev/manifest.ts` `writeDeployManifest`). Principle: Ovellum builds a portable static folder; the host deploys it, no GitHub dependency. Pinned by `manifest.test.ts` + e2e. **`--json` (C3, 2026-06-24):** machine-readable build summary (`buildSummaryToJson`, stable `{ok, command, mode, durationMs, …}` shape; mode-specific fields; warnings always present) — no decorative output on the JSON path; ConfigError emitted as `{ok:false,error,hint}`. **`--verbose` (U4, 2026-06-25):** config-resolution + per-stage / file-I/O detail to **stderr** via an `onLog` logger threaded through `runBuild`/`buildProjectDocs` (parse timing, generated/wrote/merged per file, ir/manifest paths); composes with `--json` (stdout stays pure). Also on `check`/`diff` (command-level). **IR persistence (A1, 2026-06-24):** every auto/hybrid build also writes the parsed `DocProject` to `<cwd>/.ovellum/ir.json` (envelope `{generator, format, version, project}`, `dev/ir.ts` `writeProjectIR`) — build _state_ kept at the project root beside `.ovellum/orphans/`, **unaffected by `--out`**, gitignored by the default scaffold. Foundation for `ovellum diff` (A2), rename detection (A3), and anchor last-seen (A4). Manual mode writes none. Pinned by `ir.test.ts`. |
| `ovellum diff`    | done     | **Source-vs-snapshot diff (A2, 2026-06-24).** Parses current source (pure, no writes), loads the persisted `.ovellum/ir.json`, and reports added / removed / changed / **renamed** symbols + which output docs would change. `commands/diff.ts` (CLI + human formatter) over `dev/diff.ts` `diffProjects` (pure engine). Matches by anchor id; flattens class/interface members; compares the documented surface (`signature, params, returns, description, throws, examples, deprecated, since, tags, kind, name, isExported, isInternal, isPreserved`) and **ignores `line`/`filePath`** so cosmetic shifts don't register. Maps source→doc via the generator's `outputPathFor`. `--json` (CI shape, incl. `renames`), `--exit-code` (git-diff style: exit 1 on changes; default exits 0). Missing/corrupt/format-mismatch snapshot → exit 1 with a `run ovellum build` hint; manual mode → graceful no-op (exit 0). Pinned by `diff.test.ts` (8) + cli-smoke (4). |
| Rename detection  | done (suggest-only) | **A3 (2026-06-24).** Pure `dev/rename.ts` `detectRenames(removed, added)` pairs a disappeared anchor with a similar new one — hard gate on `kind`, Levenshtein name similarity + signature-shape similarity + same-file bonus, greedy best-first 1:1 above a 0.6 confidence threshold (name floor 0.34). Surfaced in **`diff`** (likely-renames section + JSON, lifted out of add/remove) and at **build time**: when a block is orphaned but its anchor likely just moved, the build warns `did X become Y? … reattach` (`run-build.ts`, over the prior IR snapshot). Suggest-only — reattach is still manual (A4 write side). Pinned by `rename.test.ts` (6) + `rename-build.test.ts` (1) + a diff case. |
| `ovellum dev`     | done     | manual-only (HTML + live reload). Watcher ignores the output dir, `node_modules`, dot-dirs, and `ignoreFolders` (2026-06-13) — fixes an endless rebuild loop under `input: "."` where each build wrote `dist/` and re-triggered itself. **`--verbose` (U5, 2026-06-27):** opt-in request log (`METHOD path → status`, via `DevServerOptions.logRequests`, skips the SSE/reload internals). The manual-only error now points at `watch`/`build` (dropped the internal tracker ref). |
| `ovellum watch`   | done     | manual / hybrid / auto. **Incremental rebuilds (A7, 2026-06-25):** auto/hybrid keep a warm ts-morph Project (`parser` `createIncrementalParser` / `IncrementalParser`); on change the watcher re-parses only changed files, re-extracts the whole project (cheap, in-memory ASTs — so cross-file ripples are caught) and reports which `DocFile`s changed, then `runIncrementalBuild` rebuilds only those outputs via the shared `buildProjectDocs` (full + incremental share one code path → parity). `ir.json` always whole-project; hybrid zones preserved. Manual mode + config changes stay full (config reload resets the parser). Pinned by parser `incremental.test.ts` (5) + cli `incremental.test.ts` (3). |
| `ovellum serve`   | done     |
| `ovellum check`   | done     | manual / hybrid / auto. **Translation-staleness check** (2026-06-14): on i18n sites (≥2 `site.locales`), each translated page carries a `sourceHash` frontmatter fingerprint of the default-locale page it mirrors (matched by identical relative path across `content/<code>/` subtrees); `check` recomputes the source body hash (frontmatter excluded, EOL-normalized) and reports `[i18n]` stale (source changed) / missing-hash / orphan (no source) issues — any of which exits `1`. **`--update-translations`** stamps every translation's `sourceHash` to the current source (surgical single-line frontmatter upsert) and exits `0`. Implemented in `commands/check.ts` (`checkTranslations`/`stampTranslations`/`hashBody`/`upsertFrontmatterField`), reusing `walkContent` + the shared content-filter. Pinned by cli-smoke tests. **Per-locale link validation** (2026-06-14): `checkManual` now lints i18n sites locale-by-locale — each `content/<code>/` subtree builds its own locale-prefixed nav (`localeViews`), pages get locale-prefixed URLs, and links validate against the union of all locales' URLs (locale-prefixed, cross-locale-to-default, and relative all resolve). Fixed the prior false "broken link" flood on i18n sites; single-language path unchanged. **`--json` (C3, 2026-06-24):** structured `{ok, mode, pages, counts, issues[]}` (exit code unchanged); check logic extracted to exported `runCheck` so the command and the `ovellum_check` MCP tool share one implementation. **`--strict` (A6, 2026-06-25):** opt-in extra validations — `positional-zone` (id-less `@manual:start`, via reader `parseManualDoc` `hasExplicitId`), `stale-anchor` (doc `ovellum:anchor` id ∉ current IR, via `findAnchors` + `collectAnchorIds(parseProject)`), `missing-frontmatter` (page with no frontmatter `title` and no H1); tagged `[STRICT]`, counted `strict issues:` / `counts.strictIssues`; exit 1 like any issue. Threaded through `runCheck`/`checkManual`/`checkGenerated`; MCP `ovellum_check` gains a `strict` arg. Pinned by `check-strict.test.ts` (3) + e2e (the website itself is strict-clean). |
| `ovellum upgrade` | done     | npm dist-tag check; detects mgr + global/local; `--dry-run`, `--yes`. **Prefers the project's local dependency** (2026-06-14): `isLocalInstall` now also reads the cwd `package.json` (`dependencies`/`devDependencies`/`optionalDependencies`), so a project that *declares* `ovellum` is upgraded locally even when invoked via the global binary (the notes.oinam.com footgun); for a local upgrade the package manager is read from the project's lockfile (`detectManagerFromLockfile`) rather than the absent ambient user-agent, and the "Update available" line names the target (local vs global). |
| `ovellum mcp`     | done     | **MCP server (C2, 2026-06-24).** `ovellum mcp` runs a **dependency-free** stdio MCP server (hand-rolled newline-delimited JSON-RPC 2.0 in `dev/mcp/server.ts` — no SDK/zod, keeps the published CLI lean; `initialize`/`tools/list`/`tools/call`/`ping`). Tools (`dev/mcp/tools.ts`, all IR-backed, reuse the diff/orphans/query/check engines): `ovellum_query_symbol`, `ovellum_diff`, `ovellum_check` (C3, via shared `runCheck`), `ovellum_list_orphans`, `ovellum_get_page` (output-dir containment-guarded), `ovellum_build`, and the differentiator **`ovellum_write_zone`** (`dev/mcp/write-zone.ts` `applyWriteZone` — inserts/replaces a `@manual` block under an anchor id; survives hybrid regen; `dryRun`). Pure handlers + `handleMessage` unit-tested (`mcp-write-zone.test.ts` 4, `mcp-server.test.ts` 11) + stdio smoke (1). **Resources + Prompts (M1, 2026-06-25):** beyond tools, the server now advertises `capabilities:{tools,resources,prompts}` and implements `resources/list`+`resources/templates/list`+`resources/read` (`dev/mcp/resources.ts`: `ovellum://llms.txt`, `…/llms-full.txt`, `ovellum://page/{path}`, `ovellum://ir`, `ovellum://orphans`; unknown URI → -32002) and `prompts/list`+`prompts/get` (`dev/mcp/prompts.ts`: `set-up-ovellum`, `document-symbol`, `review-doc-drift`). Still hand-rolled, no SDK. **Distribution (M2, 2026-06-25):** a **Claude Code plugin** (`plugins/ovellum/` — manifest + `.mcp.json` registering `npx ovellum mcp` + the bundled `ovellum-docs` skill) listed by a repo-root `.claude-plugin/marketplace.json`, so adoption is `/plugin marketplace add oinam/ovellum` → `/plugin install`. Cross-tool install snippets (Cursor/Windsurf/Cline/VS Code) in the Automation guide. `mcp` added to the update-notifier skip list (headless-clean stdout). Registry submission is a human follow-up (TODO-Human). Pinned by `plugin.test.ts` (4). **Tools rounded out (M3, 2026-06-25):** `ovellum_search_docs` (in-process term-frequency search over the built `.md`, `dev/mcp/search.ts` — Pagefind's runtime is browser-only, so a built-in text search is used; mode-agnostic) + `ovellum_reattach` (non-interactive `orphans --reattach`: reattach to a target/suggested anchor or delete, reusing `dev/orphans.ts`). **AI-Native MCP arc complete (M1–M3).** `mcp-server.test.ts` now 13 + `search` covered. |
| `ovellum orphans` | done     | **List quarantined manual blocks (A4, 2026-06-24).** `commands/orphans.ts` over `dev/orphans.ts` (`parseOrphanFile` round-trips the merger's `renderOrphanFile`; `loadOrphans` reads `protect.orphanDir` oldest-first; `summarizeOrphans` is the pure age/stale/anchor rollup). Default lists each orphan's anchor id, doc, orphaned date + age, last-seen, block id, and — when `.ovellum/ir.json` exists — whether the anchor is `present` (reattachable) / `gone` / `unknown`. `--stale` filters to `> protect.orphanRetention` days (default 90); `--json` for CI. **A1 wiring:** `run-build.ts` reads the prior IR snapshot before overwriting it and stamps `anchorLastSeen` on freshly-orphaned blocks (prior build's `generatedAt`). **Interactive `--reattach` (A3/A4 write side, 2026-06-25):** walks each orphan and reattaches it (to a present-again or name-similar/renamed anchor via `suggestReattachTarget`, or a typed one) by splicing the prose into a `@manual` zone with the shared `applyWriteZone` then deleting the archive (`reattachOrphan`/`deleteOrphan`); delete + skip choices; TTY-guarded; reads anchors from the IR snapshot. Pinned by `orphans.test.ts` (11) + cli-smoke + e2e. |
| `ovellum clean`   | deferred |

Update notifier: after a command completes, a one-line "update available"
notice prints when the npm `latest` dist-tag is newer than the running CLI.
Cached per `update.intervalHours` (default 24h); silent in CI / non-TTY /
`NO_UPDATE_NOTIFIER` / `--no-update-check` / `update.check: false`; never
blocks or fails a run. Notice only — install is the explicit `upgrade`
command. Code: `packages/cli/src/update/{semver,registry,cache,install,notifier}.ts`.

| Flag               | Status            |
| ------------------ | ----------------- |
| `--cwd <dir>`      | done (on `build`) |
| `--config <path>`  | done (on `build`) |
| `--strict` global  | deferred          |
| `--verbose` global | deferred          |

Exit codes: `0` success · `1` build error · `3` config invalid · `2` (strict) deferred.

---

## 4. Site builder (`@ovellum/site`)

Powers `mode: 'manual'`. Design lives in [`SITE.md`](./SITE.md).

| Feature                                                                            | Status   | Notes                                                                                                                                                                                          |
| ---------------------------------------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `buildSite({ config, cwd })`                                                       | done     | Returns `{ pages, warnings, outputDir, assetsDir }`.                                                                                                                                           |
| Markdown → HTML via unified + remark + rehype                                      | done     | CommonMark + GFM (tables, strikethrough, task lists, literal autolinks) via `remark-gfm`.                                                                                                       |
| Footnotes (GFM `[^id]` references + `[^id]:` definitions)                           | done     | Parsed by `remark-gfm`; rendered as `<sup><a data-footnote-ref>` markers linking to a `<section class="footnotes">` panel with `↩` back-refs, numbered by order of first appearance (2026-06-14). **Fixed a doubled clobber-prefix bug**: `remark-rehype` prefixes the footnote id/href pairs with `user-content-`, and `rehype-sanitize`'s own `clobberPrefix` re-prefixed the `id`s (but not the `href`s), breaking every jump link — `SANITIZE_SCHEMA` now sets `clobberPrefix: ''` so the single surviving prefix matches on both ends (protection retained, applied by `remark-rehype`). The visually-hidden "Footnotes" `h2` is kept out of the ToC (`collectHeadings` skips `sr-only`) and off the autolink pass (`test` filter). Styled as a subtle tinted panel one type-step below body prose. Pinned by markdown tests (ids/hrefs resolve; label excluded from headings). |
| Heading slugs (`rehype-slug`) + clickable `#` anchors (`rehype-autolink-headings`) | done     |                                                                                                                                                                                                |
| Shiki dual-theme code highlighting                                                 | done     | `github-light` + `github-dark` via CSS variables. Zero runtime JS for highlighting. Supported langs: ts, tsx, js, jsx, json, bash, shell, markdown, yaml, html, css.                           |
| Media embeds in Markdown — `<video>`/`<audio>` + scoped `<iframe>`                  | done     | `SANITIZE_SCHEMA` permits native players (`<video>`/`<audio>` + `<source>`/`<track>`, presentational/playback attrs only; 2026-06-13) **and** `<iframe>` video embeds (2026-06-14). Iframe is narrowed twice: the schema keeps a fixed attribute set (`src`/`title`/`width`/`height`/`loading`/`referrerPolicy`/`allow`/`allowFullScreen`/`frameBorder`), then **`rehypeSafeIframe`** (post-sanitize) drops any iframe whose `src` host isn't on `IFRAME_ALLOWED_HOSTS` (`youtube.com`, `youtube-nocookie.com`, `vimeo.com`; `www.`/`player.` stripped) — relative/malformed src and every other host go. Survivors get force-set `loading=lazy` + `referrerpolicy=strict-origin-when-cross-origin` + `allowfullscreen` and are wrapped in `<div class="ov-embed">` (responsive 16:9, CSS `aspect-ratio`). Re-wrap guard mirrors `rehypeTableWrap`. **YouTube's / Vimeo's own copy-paste "Share → Embed" markup works verbatim** — the attr set mirrors theirs (incl. legacy `frameBorder`), `?si=` query strings don't affect host matching, and the responsive wrapper overrides the snippet's fixed `width`/`height`. Pinned by markdown tests (allowed hosts survive + hardened; verbatim YouTube paste survives; evil/relative/`javascript:` host stripped). |
| Auto-generated sidebar from file tree                                              | done     | Titles: frontmatter `title:` → first `# H1` → filename. When a site funnels every page through a single top-level section (e.g. all content under `/docs`), the sidebar roots at that section — its pages sit at the top level (no redundant wrapper) with the section index as the lead item, and stray root entries (404, asset dirs) drop out. Multi-section sites keep the full tree. Asset-only folders (no Markdown, e.g. `public/`) are pruned from the nav automatically (their files still copy). |
| Collapsible sidebar folders (`site.sidebar.collapse`)                              | done     | Each folder is a no-JS `<details>` disclosure with a right-aligned chevron that rotates on open (2026-06-13). **Collapsed by default**, but the branch holding the current page is rendered `open` (active item always visible). `site.sidebar.collapse: false` auto-expands the whole tree; a folder's **`_meta.json "collapse"`** overrides the global default per-folder (`false` = always open, `true` = always closed) via `NavNode.collapse`. Native `<summary>` marker hidden; section-index folders keep their link in the summary (clicking navigates — full reload — so the link/toggle overlap is harmless), pure folders show the bold group heading. Chevron right-aligns. Sub-items are indented a little under their category (`.ov-nav-children` padding) to show nesting; a folder with its own `index.md` renders as a **bold category heading link** (`.ov-nav-group--link`, 2026-06-13) so all categories read uniformly whether or not they have an index. `prefers-reduced-motion` drops the chevron transition. |
| Sidebar restores scroll to active link                                             | done     | On load, `script.js` scrolls the (independently-scrollable) sidebar so the active link is centered — full-page navigation otherwise resets the sidebar's `scrollTop` to 0 and loses your place in a long nav. Scrolls only the sidebar (never the page), no-ops when it isn't scrollable (mobile) or the link is already visible. (Fixed 2026-06-13: it read `.offsetHeight` off a DOMRect → `NaN` → never scrolled; now uses `.height`.) |
| Back-to-top button (`site.backToTop`)                                              | done     | Floating button parked above the footer via a zero-height sticky anchor; fades in after `threshold` px of scroll. **Configurable (2026-06-13):** `site.backToTop: { enabled, threshold }`, default `{ enabled: true, threshold: 360 }` (was a hardcoded 600). `enabled: false` omits the button entirely; the threshold rides on the button's `data-ov-to-top` attribute so `script.js` reads it. |
| Annotated config from `ovellum init`                                               | done     | `init` writes a fully-commented `ovellum.config.ts` (2026-06-13, was minimal `.json`): every option present — active ones set, the rest commented with defaults + allowed values — so users tinker in-file without the docs. Uses `import type { OvellumUserConfig }` + `satisfies` (erased at load → no runtime dep). Existing-config guard now matches any `ovellum.config.{ts,js,mjs,cjs,json}`. |
| Home page resolution (`index.md` / `README.md` / `site.home`)                      | done     | The `/` page resolves automatically (2026-06-13): `site.home` (explicit path) → root `index.md` → root **`README.md`**, so a repo README becomes the docs home with no config. The chosen file renders at `/` (not its own slug); the nav root uses it as its index (`buildNav` `homeBasename`); `build` and `check` agree via the shared `resolveHomeRel`. A README listed in `ignoreFiles` is skipped (opt-out). |
| README-as-index at every level                                                     | done     | A folder's index resolves to `index.*` then **`README.md`** (GitHub norm), at any depth (generalised from root-only, 2026-06-13). `build` derives page URLs from the **nav** (`flattenNav` → `sourcePath`→`url` map) instead of computing them independently, so README-as-index + permalinks render at exactly the URLs the sidebar/links use — no drift. |
| Frontmatter `permalink` + `tags`                                                   | done     | `permalink` (frontmatter) overrides a page's URL — normalized to root-absolute + trailing slash; the page renders at that path (via the nav URL map) and the nav/links follow. `tags` (string or string[]) → `<meta name="keywords">`. `title` / `description` already respected. (2026-06-13) |
| Reserved `publicDir` static-assets folder (`site.publicDir`)                        | done     | `site.publicDir` (default `'public'`) is a **reserved** folder copied to the **output root** — the SSG convention (Next/Astro/Vite/VitePress/Hugo `static`): `public/favicon.ico` → `/favicon.ico`, so root-required files (favicon, `robots.txt`, `CNAME`, OG images) land where browsers/crawlers look — and makes the `site.favicon` default work. Reserved: skipped from content/nav (no pages even from a `.md` inside) in `build`, `nav`, and `check` via a precise `publicAbs` skip; copied per-top-level-entry by a dedicated `cp` pass to the root (runs before page render → generated routes win on a same-name collision). Non-public assets still pass through preserving their path. Renamable; first of Ovellum's reserved names. (2026-06-13. Decision: root over `dist/public/` — the standard, and the only place root-only files work.) **Breaking:** pre-0.6.0 `content/public/` → `dist/public/` (generic passthrough); now → root, so drop the `/public` prefix from refs (the Ovellum website was migrated). |
| CDN base for `publicDir` (`site.assetBaseUrl`)                                      | done     | When `site.assetBaseUrl` is set (e.g. `'https://cdn.example.com/site'`), Ovellum **stops copying `publicDir` locally** (the maintainer hosts it on the CDN) and **rewrites every reference to a `public/` file** in the rendered HTML to that base: `/report.pdf` → `https://cdn.example.com/site/report.pdf`. Like Vite `base` / Next `assetPrefix` — same root-absolute authoring, only the publicDir refs move. `build.ts`: `collectPublicPaths()` enumerates the publicDir tree → root-absolute paths; `rewriteAssetUrls(html, base, paths)` (exact `"x"`/`'x'`/`(x)` value match, no partial/longer-path bleed) applied via `finalizeHtml` at landing/page/404 writes. Co-located content assets are **untouched** (they're part of the HTML site). Not rewritten: query-stringed refs and `srcset` (reference those by final CDN URL). Validated non-empty + no-whitespace. (2026-06-13.) |
| Drafts — dev-visible / prod-excluded (`draft: true`, `_meta.json "draft"`)         | done     | **v0.9.0 (2026-06-14, "the Editor" focus).** A draft is WIP: shown in `ovellum dev`/`watch` with a ribbon + sidebar badge, excluded from `ovellum build`. Marked by frontmatter `draft: true` (per page) or `_meta.json "draft": true` (whole folder, **cascades** — `NavNode.draft` propagates via `nav.ts walk`). **Repurposed** from the old "excluded everywhere" semantics (Hugo `buildDrafts` model). Plumbed via `BuildSiteOptions.includeDrafts` → `buildNav(includeDrafts, stats)` (env-gates the nav + counts exclusions) + `walkContent` skip of `_meta` draft folders in prod + `renderOne` per-page skip. Automatic by command (dev on, build off) with `build --drafts` / `dev|watch --no-drafts` overrides. Ribbon = non-sticky amber band (`.ov-draft-ribbon`, dev-only so it never touches the published monochrome); badge = `.ov-nav-draft`. Drafts excluded from sitemap/RSS always (`PageOutput.draft` filter) and never in the i18n picker. Production prints "Excluded N draft page(s) and M draft section(s)". **`draft` ≠ `ignoreFiles`**: draft is parsed + rendered (dev), ignoreFiles is never parsed. Pinned by nav + template tests; verified e2e. |
| Content exclusion (ignoreFolders / ignoreFiles / hidden / draft / auto)            | done     | Keep content out of the manual-mode build (nav **and** output): `site.ignoreFolders` (folder names, any depth); **`site.ignoreFiles` (file globs — pages AND passthrough assets; 2026-06-13)** via the shared `content-filter.ts` glob (`*`/`**`/`?`, gitignore-style basename-vs-path); `_meta.json "hidden": true`; frontmatter/`_meta` `draft` (now **dev-visible/prod-excluded** — see the Drafts row). **Auto-excluded with no config** (so `input: "."` doesn't leak): dotfiles, `node_modules`, package manifests/lockfiles, the `ovellum.config.*`, and the **output dir itself** (kills `input:"."` self-recursion). Centralised in `content-filter.ts` and shared by `build.ts` `walkContent`, `nav.ts` `walk`, **and `ovellum check`** — so the three can't drift. `public/`-style asset folders still auto-prune from the nav. |
| `_meta.json` per-directory override                                                | done     | Sets directory `title` and `order` (slug list).                                                                                                                                                |
| Right-side "On this page" ToC                                                      | done     | h2/h3 only.                                                                                                                                                                                    |
| Pretty URLs (`name/index.html`)                                                    | done     |                                                                                                                                                                                                |
| Static asset passthrough                                                           | done     | Non-`.md` files (images, etc.) copied as-is.                                                                                                                                                   |
| **Landing page** (`site.landing.enabled`)                                          | done     | Disabled by default. When enabled: full-width hero + feature grid + optional `_landing.md` prose + optional trust strip rendered at `/`. Doc pages keep their URLs. Top-bar gains a Docs link. Feature grid renders as subtle cards via the reusable `.ov-card` primitive (`--color-surface` + hairline border + `--radius-lg`). **Linkable feature cards (2026-06-26):** `features[].href` makes the whole card an `<a>` — site-relative (locale-prefixed via `localizeHref`) or absolute (new tab, `rel=noopener`); `.ov-feature-card-link` resets link styling + adds a calm hover (firmer border + faint lift). The site's own landing uses it to link all 6 cards into `/docs` (AI-Native + MCP highlighted). **Composable sections (B5, 2026-06-27):** `landing.sections: OvellumLandingSection[]` (union `hero|install|features|trust|scene|prose|custom-html`) takes over the fixed `hero→install→features→prose→trust` order — arrange/repeat blocks and inject `prose` (inline `html` or the `_landing.md` body) or `custom-html` (author-trusted raw section, `.ov-landing-html`) anywhere. Flat config stays the data source for the reference blocks (shorthand); unset = byte-identical default order (scenes auto-interleaved). Inline prose/custom-html isn't localized (flat blocks are). `renderLanding` switch; pinned by landing (4) + validate (1) tests. |
| Topbar appearance control (mode + palette + accent + text size + font)             | done     | Palette-icon popover (inlined into the mobile sheet) replacing the old cycle toggle (2026-06-12). **Text size** and **Font** controls added 2026-06-14 (see their own rows below). **Mode**: auto/light/dark segmented control → `data-theme`. **Theme**: five bundled palettes — Ovellum (the monochrome base, id `default`, pinned first), then alphabetical E-ink (warm paper + ink black), Flexoki, Nord, Solarized → `data-palette`; each is shown with a crisp monochrome Lucide line glyph (pen-tool for Ovellum — a nib echoing the brand mark / book-open / feather / snowflake / sun-dim) rather than the old color-swatch dot. Each palette re-skins the gray ramp in `style.css`, so light + dark come free from the reversed-ramp remap (palette blocks sit before the dark blocks; per-palette dark accents after, at higher specificity). macOS was dropped 2026-06-13. **All values in OKLCH** per the design principle (canonical palette hex kept in trailing comments as the fidelity reference); shipped CSS stays all-OKLCH because `scripts/build-templates.mjs` minifies CSS with whitespace/identifier passes only — esbuild's `minifySyntax` would rewrite in-gamut `oklch()` back to hex, so it's deliberately off for CSS (kept on for JS). **Color** (renamed from "Accent"): six presets + native color input + a leading "Default" swatch (filled with `--color-fg`, clears the override) → inline `--ov-accent` + `data-accent` on `<html>`; the override block drives **both** the primary role (CTA buttons) and the accent role (links/focus/ToC), hover via `color-mix`, with a repeated-attribute specificity bump to beat palette+mode combos. All five (mode/palette/color/text-size/font) persisted in localStorage (`ovellum-theme` / `ovellum-palette` / `ovellum-accent` / `ovellum-text-size` / `ovellum-font`) and applied pre-paint by the boot script, which also owns the per-palette bg map driving Safari's `theme-color` (`window.__OV_PALETTE_BG__`; a stale stored palette like the removed `macos` degrades gracefully to default). Config defaults: `site.defaultTheme`, `site.palette`, `site.accent`. |
| Optional brand logo (`site.logo`)                                                  | done     | `site.logo` (path/URL) renders a mark before the title; **unset = no mark, title alone** (logo never mandatory; 2026-06-13 — replaces the previously hardcoded Ovellum brand SVG that was wrongly shipping into every user site). Drawn as a CSS-masked `currentColor` silhouette (`-webkit-mask-image`/`mask-image` inline, validated to exclude `url()`-breaking chars) so it flips with the theme like the rest of the monochrome chrome — provide a single-color SVG/PNG. Decorative (`aria-hidden`); the title carries the accessible name. basePath-aware. |
| Favicon (`site.favicon`)                                                           | done     | `<link rel="icon">` emitted on every page (2026-06-13). Defaults to a root `/favicon.ico` (drop one at the project root → passthrough → works); override with `site.favicon` to any path/URL. basePath-aware. |
| Version badge next to the brand                                                    | done     | Driven by `site.version` (free-form string, e.g. `"v0.2.1"`). Rendered as a small mono chip baseline-aligned with the brand title. Omitted when `site.version` is unset.                       |
| Right-aligned topbar nav with mobile sheet                                         | done     | Driven by `site.topbarNav`. Brand (optional `site.logo` mark + wordmark + version) on the left; the search box fills a centered zone; on the right, text links → icon-only links (via `icon`) → divider → appearance control, each a rounded-square button. External text links get a small SVG arrow. Below 720px the top row keeps logo + version + centered search + hamburger; the nav and the appearance panel move into the full-width sheet (icon links regain their label there). |
| Two-column footer (copyright + footerNav)                                          | done     | `site.footerNav` populates the right column; items with `icon` render as icon-only Lucide glyphs (`github` / `rss` / `mail` / `package`), items without as text. Left column = `site.footer` text (+ build date) and an optional **"Built with Ovellum" credit link** (`site.credit`, default `true`, → ovellum.oss.oinam.com; set `false` to remove — 2026-06-13). `site.footer` now defaults to `''` (the credit is the default attribution). Footer outer boundary tracks the topbar's max-inline-size and gutters so all three rails align. |
| Inline SVG icon registry (Lucide)                                                  | done     | `renderIcon(name)` returns a 24×24 currentColor SVG with Lucide's canonical attributes (stroke-width 2, round caps). Path data imported per-icon from `lucide` (tree-shaken: ~100B per icon). Public set: menu, close, sun, moon, monitor, chevron-down, github (hand-rolled — Lucide v1 dropped brand marks), external-link, search, check, copy, rss, mail, package, palette, and the appearance-panel theme glyphs pen-tool / book-open / feather / snowflake / sun-dim. |
| Hero with dotted-noise + spotlight bg                                              | done     | Default landing hero: two stacked pseudo-elements (a 24px dotted SVG pattern + a radial accent spotlight). No images shipped; both data-URL/CSS-only. Suppressed when `site.landing.hero.media` is set.                                                |
| Imagery hero (`site.landing.hero.media`)                                           | done     | Opt-in full-bleed visual layer behind the title/subtitle/CTAs. Stacks two `<img>` tags (light + dark) and toggles them via `[data-theme]` so the page-level theme switch flips assets without JS. Bottom edge softly fades into the page via CSS mask. Animation, theme-respecting fills, and `prefers-reduced-motion` handling live **inside** the SVG asset itself — swap the file to change motion or palette. |
| Landing install snippets (`site.landing.install`)                                  | done     | Optional titled command snippets spliced between the hero CTAs and the feature grid. Each `{ title, code, lang? }` runs through the same markdown/shiki pipeline as doc code blocks (highlighting + `data-language` + `data-copy`) and the section is wrapped in `.ov-prose`. Inside `.ov-install` the language eyebrow is suppressed and the copy button is rendered as an icon, vertically centered on the right edge (copy glyph → check); doc code blocks elsewhere keep their eyebrow + text copy button. `lang` defaults to `bash`. |
| Section scenes (`site.landing.scenes`)                                             | done     | Optional ambient visuals interleaved between landing sections in order. Each scene is a centered figure (inherits landing's `--page-max`, 16:9 aspect, `object-fit: contain`) with top/bottom mask-fade. Bundled scenes are hand-authored SVGs with named groups so per-element animation (windmill blades, drones, leaves, ripples, etc.) lives **inside** each asset alongside its own `prefers-reduced-motion` handling — mirrors the hero pattern. Light/dark variants follow the same `[data-theme]` flip as the hero. `aria-hidden` by default; set `alt` to opt into AT announcement. |
| Copy button on code blocks                                                         | done     | Injected client-side; vanilla JS. Docs code blocks get a text **Copy** button (top-right, hover-revealed, label → "Copied"). Install snippets (inside `.ov-install`) get an icon button instead (copy glyph → check on success) vertically centered on the right edge; subtle by default, full-strength on hover / keyboard focus. Icons (`copy`/`check`) live in the icon registry. Install snippets carry a `data-copy-text` attribute (set in `build.ts`) so the copy button yields only the command, never the folded-in title comment; docs blocks have no such attribute and copy full `innerText`. |
| Language eyebrow on highlighted code blocks                                        | done     | CSS `::before attr(data-language)` uppercase label in the top-right of every doc code block; hover-fades so it doesn't fight the copy button. Suppressed inside `.ov-install` (install snippets stand alone with just the icon copy button). `data-language` is emitted by the markdown pipeline (drives syntax highlighting + the comment prefix). |
| Default light + dark themes                                                        | done     | Token architecture (2026-06-06): one generic gray ramp `--color-gray-50…950` (+ white/black, Tailwind-"neutral" values) → role triples (`--color-primary/-secondary/-accent` ×value/-fg/-hover) → semantic surfaces/text. All color lives in `style.css` (not synced from STYLES.md). Dark = the same ramp remapped to reversed steps (one block, no separate color values). |
| Monochrome CTA buttons                                                             | done     | Landing CTAs read the `--color-primary` / `--color-secondary` role triples (no separate `--color-cta-*` set). Default theme: primary = dark gray fill, secondary = light fill + hairline; dark remaps to a white primary + recessed `gray-800` secondary. Fully monochrome, all from the gray ramp. |
| Monochrome chrome + editorial frame                                                | done     | Site-wide redesign (2026-06-04). **Fully monochrome in both themes** — chrome AND callouts are neutral; the only color on a page is code syntax highlighting. `--color-accent`/`--color-link` resolve to `--color-fg`, focus ring to neutral gray; callout type tokens are fg-derived (`--color-fg` rule/label on `--color-bg-subtle`), defined once in `:root` and auto-adapting to dark (dark blocks no longer redeclare accent/link/callouts). Borders are translucent `color-mix` tints of `--color-fg`. A fixed, `pointer-events:none` `.ov-frame` (`renderFrame()`) draws two faint `--color-frame-line` vertical rules hugging the content edges, marked at the header baseline by a small darker **`+`** (a 10px box of two crossing 1px `--color-fg-subtle` gradients — no fill/border, so it reads as a quiet cross not a dot); the **footer mirrors the `+`** at its top border via `.ov-footer-inner::before/::after`. Aligned to `--chrome-max` so rules + marks match topbar/footer edges on every page; hidden below 720px and in print. Header is a fixed-height (`--ov-header-h: 4rem`) frosted bar. System-font + OKLCH principles unchanged. |
| Boxed content card on doc pages                                                    | done     | The reading column (breadcrumbs + page-meta + article + edit link) is wrapped in `.ov-content-card`: a light `--color-surface` box with a hairline border, `--radius-lg` corners, and a soft drop-shadow, lifted off the body (coss-style). Width is capped via `--content-card-max` (prose measure + card padding) so the box hugs the reading column; the prose's own cap is released inside. Prev/next sits below the card (sibling, same width cap, no top rule). Neutralized on the centered 404 and in print; landing pages keep their own section layout. |
| Body font picker + bundled webfonts (`site.font`)                                  | done     | `site.font: 'sans' \| 'serif' \| 'inter' \| 'geist'` (default `'sans'`, 2026-06-14) sets the initial `data-font` on `<html>` and seeds the in-panel **Font** picker; `[data-font=…]` swaps the `--font-body` role (body + headings + prose follow; code stays `--font-mono`). `sans`/`serif` are system stacks (zero webfont). **`inter`/`geist` are variable webfonts bundled in the template** (`templates/default/fonts/{inter/InterVariable.woff2 344KB, geist/Geist-Variable.ttf 165KB}`) — `writeStaticAssets` copies the `fonts/` dir to `dist/assets/fonts/`, and the `@font-face` rules (`'Inter var'`/`'Geist var'`, url relative to ovellum.css) are **lazy by spec**: a file is fetched only when a page actually renders in that family, so the default stays fast and a custom font costs only on opt-in. Picker previews each option in its own face (the preview is what pulls the font, and only once the panel — `display:none` until then — is opened). Live swap via the panel; no reload. Applies to docs + landing. The website dropped its `headExtra` typeface hack and dogfoods the `'sans'` default. **Custom fonts (B4, 2026-06-27):** `site.font` also accepts `{ body, mono?, source?, label? }` for a self-hosted family — `<html data-font="custom">` + an injected `[data-font="custom"]` rule (after the base CSS, under `[data-font]` so the picker still wins) + `source` stylesheet `<link>`(s) + a custom picker entry previewed in its own family. FOUT is the author's `@font-face` `font-display`. Values sanitized (`< > { } ;` stripped/rejected); `source` must be http(s)/relative. `OvellumCustomFont` (core) + `renderCustomFontHead` (template); pinned by template (2) + validate (1) tests. |
| Reader text-size scale (5 steps)                                                   | done     | Appearance-panel **Text size** control (2026-06-14): five steps rendered as a graduated "A" ramp (no text labels) — two smaller, default in the middle, two larger — like Kindle / Safari Reader. Writes `data-text-size` (xs\|s\|m\|l\|xl) on `<html>`; CSS maps each to a `--ov-text-scale` multiplier folded into `--font-size-0`, so the **whole modular type scale** (body + every heading + prose) scales proportionally. Persisted (`ovellum-text-size`), applied pre-paint, default `m` (untouched 1×). Range kept modest (0.875–1.125) so the fixed header chrome never breaks. |
| Quiet sidebar + roomier content (2026-06-04)                                       | done     | Sidebar has **one** left border — the frame's left rail itself (no separate track). `.ov-layout` uses asymmetric inline padding (left `--frame-inset` flush onto the rail; right `--frame-inset + --frame-gutter` so the ToC clears the right rail). Group headings + links (incl. nested children) share one flush-left edge with a minimal `--space-2xs` inset — a flat list, no indent hierarchy. The active item reads via darker color (`--color-fg`, not bold) + a 2px `--color-fg` `::before` strip that lands on the rail. Column gaps widened to `--space-xl`. Prev/next hover darkened (foreground-strength border + `--color-bg-muted` fill). Search input rounds to `--radius-xl` (0.75rem). |
| Nord / Solarized themes in switcher                                                | done     | Shipped as palettes in the appearance control (plus Flexoki + E-ink, beyond the original plan; macOS was trialled then dropped 2026-06-13). Implemented as ramp re-skins, not the STYLES.md §7 per-token blocks — see the appearance-control row above. |
| Footer with build timestamp                                                        | done     | Configurable; empty string disables.                                                                                                                                                           |
| Canonical `<link>` + OG meta                                                       | done     | When `site.baseUrl` is set.                                                                                                                                                                    |
| Raw `<head>` injection (`site.headExtra`)                                          | done     | Verbatim HTML injected into `<head>` on every page, after the search bits and before the inline theme-boot script. Not escaped/sanitised — author-controlled markup only. Unset by default. Primary use: analytics snippets. |
| `site.basePath` (Jekyll-style sub-path hosting)                                    | done     | Prepended to every internal URL, asset path, canonical link, sitemap entry. Empty by default.                                                                                                  |
| Multiple languages / i18n (`site.locales`, `site.defaultLocale`)                   | done     | **Opt-in content i18n (v0.8.0, 2026-06-14).** `site.locales: [{code,label}]` (BCP 47 codes) + optional `site.defaultLocale`. Unset → single-language path **byte-for-byte unchanged** (290 existing tests stayed green). Set → content moves to `content/<code>/` subtrees; `defaultLocale` serves at root, others under `/<code>/`. `build.ts` runs a **two-phase per-locale pass**: Phase A builds each locale's nav (URLs prefixed via `prefixNav`) + a translation-key map (prefix-stripped URL → URL); Phase B renders with cross-locale knowledge. Pages map across languages by **identical relative path**. **Language picker** = no-JS `<details>` globe dropdown in the topbar (after nav links, before the divider; mirrored in the mobile sheet) of autonyms; switching → the page's equivalent in that locale, else **falls back to that locale's home** (partial translations fine). Per page: `<html lang>`, `hreflang` + `x-default` (absolute, when `baseUrl` set), per-locale `/<code>/404/` (default keeps root `404.html`). `publicDir` stays **shared** (copied to root once). Pinned by validate + template tests; verified e2e. **Per-locale RSS shipped 2026-06-27** (see the RSS row) — no known i18n gaps remain. (Chrome/UI-string localization + `<html dir>`/RTL, the translation-staleness check, per-locale `check` link validation, and config-text localization all shipped 2026-06-14 — see their own rows.) |
| i18n config-text localization (`LocalizedString`)                                  | done     | **2026-06-14.** Config-driven user-facing strings accept a per-locale map (`string \| Record<localeCode, string>`, type `LocalizedString` in core): `topbarNav`/`footerNav` `label`, and the `landing` hero `title`/`subtitle`, CTA `label`, feature `title`/`description`, install `title`, trust-strip `label` + item `name`. Resolved at render via `localize(value, lang, site.defaultLocale)` in `template.ts` (landing + nav) and `build.ts` (install-title fold) — plain strings pass through so single-language output is byte-identical. Validator (`config/validate.ts` `isLocalized`) accepts string-or-map for those fields. The website's own landing + nav ship en/ja maps. Pinned by `strings.test.ts` + landing tests. |
| i18n chrome / UI-string localization (`strings.ts`)                                | done     | **2026-06-14.** Every hardcoded template UI string ("On this page", "Edited" + dates, "min read", appearance-panel labels, prev/next, 404, back-to-top, breadcrumb, draft ribbon, nav aria-labels, copy-button labels) resolves through a per-locale `UiStrings` table in `packages/site/src/strings.ts`: `DEFAULT_STRINGS` (English) → `BUILTIN_STRINGS[baseLang]` (ships **en + ja**) → per-locale `locales[].strings` override. `resolveStrings(undefined)` === `DEFAULT_STRINGS`, so the **single-language path stays byte-for-byte identical** (no `dir` attr, no `__OV_I18N__` script). Threaded as `strings`/`dir` through `template.ts` (ShellOptions + page/landing inputs + sub-renderers); dates use `Intl.DateTimeFormat(locale, …, {timeZone:'UTC'})` + `strings.today/yesterday` (English still `Jun 14, 2026`); runtime copy-button labels injected via `window.__OV_I18N__` (only when non-English). RTL base languages (`ar`/`he`/`fa`/…) get `<html dir="rtl">`. Proper nouns (palette + font names) stay literal. Pinned by template tests (ja chrome present; `dir="rtl"` only when rtl). |
| Prev / next page navigation                                                        | done     | Auto-rendered at the bottom of each doc page from the sidebar order.                                                                                                                          |
| Minified CSS + JS                                                                  | done     | The default template's `style.css` / `script.js` ship **minified** in `dist/assets/ovellum.{css,js}` (~42% / ~56% smaller; comments stripped). Done at package-build time via `scripts/build-templates.mjs` (esbuild `transform`, a dev-only dep) while copying `src/templates → dist/templates`; `ovellum build` stays a plain copy, so no minifier ships to users and the source files stay readable. |
| `sitemap.xml`                                                                      | done     | Auto-generated when `site.baseUrl` is set. `/404/` excluded by default. `<lastmod>` from git mtime deferred.                                                                                  |
| Search (Pagefind)                                                                  | done     | Opt-in via `site.search.enabled`. `ovellum build` runs the Pagefind indexer against `dist/` and the topbar mounts the default Pagefind UI (themed via CSS variables to match Ovellum's tokens). The search input carries a subtle `--color-bg-muted` fill (2026-06-14) so it reads as a distinct field against the topbar/page background in both modes. |
| Breadcrumbs                                                                        | done     | Auto-rendered above the article when the page sits at least two levels deep in the nav (top-level pages get none). Last entry marked `aria-current="page"`.                                  |
| Custom 404 layout                                                                  | done     | `/404/` gets `body.ov-body-404`: sidebar, ToC, prev/next, breadcrumbs, and edit-this-page are hidden; the article centers on a narrower column with a larger heading. Excluded from the reading flow — sidebar, sitemap, RSS, **and prev/next** (`findAdjacent` filters `/404/`, so the first real page's "Previous" is empty, not the 404). Documented user-facing in the [manual-mode guide](https://ovellum.oss.oinam.com/docs/guides/manual-mode/#the-404-page). Core `ovellum build` emits **both** `dist/404/index.html` and the top-level `dist/404.html` (since 2026-06-06), so the custom 404 triggers on GitHub Pages/Netlify/Cloudflare with no extra step — the old website-only `scripts/website-postbuild.mjs` copy was retired. **Always generated (2026-06-13):** if `content/404.md` is absent, a default "Page not found" page (template-matched, link home) is synthesised — every site ships a 404, authored or not. It's infrastructure, so it's not counted in the build's page total. |
| Print stylesheet                                                                   | done     | `@media print` hides chrome (frame, topbar, sidebar, ToC, search, prev/next, edit link), widens content to full width, prints external link URLs inline, and avoids page-breaks inside code blocks. |
| Reading time + last-modified ("Edited")                                            | done     | Per-page meta line above the article: `N min read · Edited <date>` (label renamed from "Updated" 2026-06-14). Word count strips code blocks/HTML; ~200 wpm. Date resolution (0.10.0): frontmatter **`updated:`** pins it explicitly (`normalizeFrontmatterDate` — YAML Date or string; unparseable → warn + fall back) → else **`git log --follow --diff-filter=AM`** (follows renames + ignores pure moves so a `git mv` doesn't reset "Edited"; replaced the plain `git log -1` that made every moved file read "today" after the 0.8.0 i18n content move) → else fs mtime. The date's wording follows **`site.dateFormat`**: `'humanized'` (default) → `today` / `yesterday` (relative to build time) / `Jun 14, 2026`; `'iso'` → `2026-06-14`. Formatted by `formatEditedDate()` in page-meta.ts (UTC day-diff so timezone can't flip today/yesterday; unparseable → raw ISO); the machine-readable ISO always stays in `<time datetime>`. Toggle each half via `site.pageMeta.{readingTime,lastModified}`. |
| RSS feed (`feed.xml`)                                                              | done     | Auto-generated when `site.baseUrl` is set. Items sorted by `lastModified` desc, capped at 20; `/` and `/404/` excluded; channel-level `<atom:link rel="self">`; head `<link rel="alternate">` on every page for auto-discovery. **Per-locale (2026-06-27):** on i18n sites each locale gets its own `<prefix>/feed.xml` (`/feed.xml` + `/ja/feed.xml`), scoped to that locale's pages, with the channel/self link prefixed (`generateRss` `localePrefix`; `build.ts` `localeForUrl` groups pages by longest matching prefix). Sitemap stays one combined file. Single-locale output byte-identical. Pinned by `rss.test.ts` (9). |
| AI-friendly output (`llms.txt` / `llms-full.txt` / `.md` mirrors)                  | done     | **C1, AI-Ready theme (2026-06-14).** Emits machine-readable companions to the HTML so agents/LLMs read the docs cleanly, per the [llmstxt.org](https://llmstxt.org) convention. Three artifacts, gated by **`site.ai`** (`{enabled?,llmsTxt?,fullText?,mdMirror?}`, resolved by `resolveAiConfig` in `packages/site/src/llms.ts`): **`/llms.txt`** — link-first index (`- [Title](link): description`) in **sidebar order** (`flattenNav`), default **on**; **`/llms-full.txt`** — whole corpus concatenated, default **off** (can be large); per-page **`.md` mirror** at `<page>.md` (`/guide/intro/` → `/guide/intro.md`, `/` → `/index.md`; `mdMirrorPath`), default **on** — `renderOne` now returns the raw `parsed.content` to feed it. When `mdMirror` is on, `llms.txt` links point at the mirrors. Drafts + 404 excluded (same rule as sitemap/RSS); **per-locale** on i18n sites (`/ja/llms.txt`, `/ja/**/*.md`). HTML output byte-identical; these are additive files. `enabled:false` turns all of it off. Pinned by `llms.test.ts` (10 cases) + e2e website build. |
| Callouts (GitHub alert syntax)                                                     | done     | `> [!NOTE]`, `> [!TIP]`, `> [!IMPORTANT]`, `> [!WARNING]`, `> [!CAUTION]` blockquotes render as labeled panels via a rehype plugin. Editorial-calm styling: 3px left rule + uppercase eyebrow + faint type-color tint. Tokens (`--callout-{type}-{fg,bg}`) live in style.css and pull from STYLES.md palette ramps via `pnpm extract-tokens`. |
| **Component directives (B2, 2026-06-26)**                                           | done     | Markdown-native block components via `remark-directive` (`:::name`) — **not** JSX/MDX, so output stays portable Markdown. `packages/site/src/directives.ts`: **`remarkComponents`** (remark phase) maps directives to class-tagged `<div>`/`<a>`; **`rehypeTabs`** (post-sanitize, like `rehypeCallouts`) upgrades `:::tabs` into an accessible tablist (button/role/aria/id, trusted because post-sanitize). Set: **callouts** `:::note\|tip\|important\|warning\|caution{title}` (alias the existing `.ov-callout` styles), **`:::steps`/`:::step{title}`** (CSS-counter stepper), **`:::cards`/`:::card{title,href}`** (responsive grid; `href`→linkable `<a>` card), **`:::tabs`/`:::tab{label}`** (tablist + panels; `script.js` toggles + arrow-keys; no-JS shows all panels). Component classes whitelisted (value-restricted) in `SANITIZE_SCHEMA` `*` + a rebuilt `a` className rule (element rule overrides `*`). Labels are `<div>` (not headings → no ToC pollution). Nested containers need one extra colon (`::::steps` > `:::step`). Pinned by `markdown.test.ts`. Docs: `guides/components` (en+ja). **Mermaid diagrams (2026-06-26):** ` ```mermaid ` fences → `<pre class="mermaid">` (`rehypeMermaid` in `directives.ts`); the runtime is **lazy-loaded client-side, only on pages with a diagram** (script.js reads `<html data-ov-mermaid>`), from a pinned jsDelivr build by default. Config `site.mermaid: { enabled?, url? }` (disable, or self-host the runtime). No-JS/offline = readable source fallback. **Per-page LLM actions (Copy-for-LLM, 2026-06-26):** when the `.md` mirror is on, each doc page gets a `.ov-page-actions` row — **Copy page** (script.js fetches the `.md` + clipboard), **View as Markdown**, and (when `site.baseUrl` set) **Open in ChatGPT / Claude** (deep-link with a prompt pointing at the absolute `.md`). `renderPageActions` in template.ts; build passes `markdownUrl`; i18n strings added. **B2.2 (2026-06-26):** `:::code-group` (tabbed code blocks — each fence → a tab labeled by its lang or `title="…"`; reuses the `.ov-tabs`/`rehypeTabs` path, shiki highlights after; single `:::` since children are fences) + **`.mdx`-as-Markdown** (widened the `isMarkdown`/`stem`/`urlFor` regexes in `nav.ts`+`build.ts` to `md|markdown|mdx`; `.mdx` files are routed/rendered as Markdown, **no JSX eval**). `markdown.test.ts` (6 directive cases). **B2 COMPLETE.** |
| MDX rendering                                                                      | deferred | `.md` only in v1.                                                                                                                                                                              |
| Multiple bundled templates                                                         | deferred | One default for now.                                                                                                                                                                           |
| Live reload                                                                        | deferred | Pairs with `ovellum watch`.                                                                                                                                                                    |
| Plugin API for custom templates                                                    | deferred |                                                                                                                                                                                                |

**Template anatomy**: where the bundled template lives, what format each
piece is in, and how to change the design — see
[`SITE.md` §9a](./SITE.md#9a-template-anatomy).

**Tests:** ~100 vitest cases in `@ovellum/site` (markdown incl. sanitization + callouts + language-label tagging, nav, template, landing incl. imagery-hero variant + scene interleaving, sitemap, rss, url helpers, page-meta incl. command-injection resistance, icons) plus the `ovellum` CLI suite (URL-scheme allowlist, dev-server path-traversal defense, spawn-based smoke tests, and an in-process **merge-survival golden test** driving parser → generator → reader → merger → orphan-writer end-to-end).

**Coverage:** `pnpm test:coverage` (root-only `vitest.coverage.ts`, `@vitest/coverage-v8`) aggregates the whole workspace into one report — baseline ~65% lines / 76% branches. The CLI command layer and `site/build.ts` read low only because the spawn-based smoke tests run them in a subprocess v8 can't instrument; they are exercised, not uncovered.

**Security:** sanitization policy, shell-out hardening, and URL-scheme allowlist are documented in the [security reference](https://ovellum.oss.oinam.com/docs/reference/security/) (defenses-in-code map in [`DESIGN.md`](./DESIGN.md#where-the-security-defenses-live)).

---

## 5. Design tokens (`STYLES.md`)

Authoritative reference for color, type, space, rhythm. Site-builder
stylesheet **hand-ports** from this.

| Token group                                                | Status                                    |
| ---------------------------------------------------------- | ----------------------------------------- |
| OKLCH palette: 4 neutrals + 8 accents, 50–950              | done                                      |
| Type scale (Major Third → Perfect Fourth, fluid)           | done                                      |
| Space scale (Utopia static + fluid pairs)                  | done                                      |
| Themes: default light + dark                               | done                                      |
| Themes: Nord (light + dark)                                | partial (in STYLES.md; not in stylesheet) |
| Themes: Solarized (light + dark)                           | partial (in STYLES.md; not in stylesheet) |
| Token-extraction script (auto-sync stylesheet ← STYLES.md) | deferred                                  |

---

## 5.5 Programmatic API (`ovellum` library entry)

**D2 (2026-06-25).** `import { build, watch, loadConfig, defineConfig } from 'ovellum'` — a curated, **side-effect-free** facade for driving Ovellum in-process (framework dev servers, monorepo tasks, custom build steps). `packages/cli/src/api.ts`. `build(opts) → BuildSummary` (opts: cwd/configFile/out/base/drafts/manifest/onLog), `watch(opts) → ActiveWatcher{close()}` (cwd/configFile/drafts/onBuild/onError; incremental in auto/hybrid), `loadConfig(opts)`. Packaging: **split entry** — `bin`→`dist/index.js` (CLI, shebang, runs `runMain`), `main`/`exports["."]`→`dist/api.js` (library, no shebang). **ESM-only** (`type: module`; pagefind is ESM-only). Self-contained `.d.ts` via tsup `dts:{resolve:[/^@ovellum\//]}` (inlines the private `@ovellum/*` types; `tsconfig.dts.json` non-composite for the dts pass). Curated facade over `runBuild`/`watchAndBuild` — raw `@ovellum/*` stay private. Also fixes config-file type autocomplete (`import type { OvellumUserConfig } from 'ovellum'` now resolves). Pinned by `api.test.ts` (5).

## 6. Examples

| Fixture                                                | Mode     | Demo command            | What it shows                                                                                                               |
| ------------------------------------------------------ | -------- | ----------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| [`examples/simple-ts/`](../../examples/simple-ts/)     | `hybrid` | `pnpm -w run demo`      | Two TS files → Markdown docs; protected `@manual` blocks survive regeneration; orphans quarantine when a symbol disappears. |
| [`examples/manual-site/`](../../examples/manual-site/) | `manual` | `pnpm -w run demo:site` | Five-page static site with sidebar, right ToC, syntax-highlighted code, auto/light/dark toggle.                             |

Generated outputs are gitignored per-example.

---

## 6.5 Official website (`website/`)

Ovellum's own site, built with Ovellum in manual mode and deployed to
GitHub Pages on every push to `main`. Lives in
[`website/`](../../website/). Design lives in [`DEPLOY.md`](./DEPLOY.md).

| Feature                                     | Status   | Notes                                                                                                              |
| ------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------ |
| Landing + full docs tree built from Markdown | done    | `pnpm -w run build:website` → `website/dist/`. Includes the AI-Ready surface: `guides/automation` (C3) + `concepts/ai-ready` (C5, "Ovellum for AI agents" positioning). |
| Custom domain via `CNAME`                   | done     | `website/content/CNAME` passes through to `dist/CNAME`. Target: `ovellum.oss.oinam.com` (DNS pending; TODO-Human). |
| `404.html` for missing paths                | done     | Always emitted by core `ovellum build` (since 2026-06-13): `content/404.md` → `dist/404/index.html` **and** root `dist/404.html`; if absent, a default template-matched 404 is synthesised. The file GitHub Pages/Netlify/Cloudflare serve for missing paths (a platform that serves its own simply ignores ours). |
| Deploy workflow (`deploy-website.yml`)      | done     | Push to `main` → build → `actions/deploy-pages@v4`. Concurrency-cancellation enabled.                              |
| PR preview workflow (`website-preview.yml`) | done     | Pull-request builds upload `website-dist` as an artifact; no deploy.                                               |
| pnpm + Node cache in CI                     | done     | `actions/setup-node@v4` with `cache: pnpm`.                                                                        |
| `site.basePath` for subpath hosting         | deferred | Needed only if hosting from `<user>.github.io/<repo>/` instead of a custom domain.                                 |
| Pagefind search integration                 | deferred | Post-build indexer + ~50 KB client. Separate slice.                                                                |
| Sitemap.xml / RSS                           | done     | Both auto-emit when `site.baseUrl` is set. RSS items sorted by lastmod desc, capped at 20.                          |
| Lighthouse CI                               | done     | `.github/workflows/lighthouse.yml` runs `@lhci/cli autorun` against the built site on each PR; report uploaded as artifact. Informational only — no merge gate. |

---

## 7. Project plumbing

| Feature                                          | Status | Notes                                                                                                                                                                      |
| ------------------------------------------------ | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| pnpm workspaces + Turborepo                      | done   | Topological build / test / lint / typecheck.                                                                                                                               |
| TypeScript project references                    | done   | Every package `composite: true` with `tsBuildInfoFile` inside `dist/`.                                                                                                     |
| Build pattern `tsup && tsc -b --force`           | done   | Required for multi-file packages with composite refs. Documented in [`TODO.md` Phase 1 build note](./TODO.md#phase-1---core-types--config-ovellumcore) and project memory. |
| ESM-only for `@ovellum/site`                     | done   | Uses `import.meta.url` to resolve bundled template dir.                                                                                                                    |
| Post-build asset copy (site templates → `dist/`) | done   | `node -e "require('fs').cpSync(...)"` step.                                                                                                                                |
| Prettier                                         | done   | `pnpm format` / `format:check`.                                                                                                                                            |
| ESLint flat config + typescript-eslint           | done   | `src/templates/**` excluded for browser-globals.                                                                                                                           |
| changesets                                       | done   | Configured; no releases yet.                                                                                                                                               |
| GitHub Actions CI                                | done   | `ci.yml` (lint + typecheck + test + build) and `release.yml` (changesets publish).                                                                                         |
| Demo scripts                                     | done   | `demo`, `demo:clean`, `demo:site`, `demo:site:clean`, `build:website`, `build:website:clean`.                                                                              |
| Website deploy + PR preview workflows            | done   | `.github/workflows/{deploy-website,website-preview}.yml`. See [`DEPLOY.md`](./DEPLOY.md). (The old `scripts/website-postbuild.mjs` 404 copy was retired once core started emitting `dist/404.html`.)                                                |

---

## 8. Tests at a glance

| Package              | Cases  | What's covered                                                                      |
| -------------------- | ------ | ----------------------------------------------------------------------------------- |
| `@ovellum/core`      | 29     | config loading, merge, validation, frontmatter overrides                            |
| `@ovellum/parser`    | 6      | symbol types, filtering, `@preserve`/`@deprecated`                                  |
| `@ovellum/generator` | 4      | function rendering, output paths, multi-file                                        |
| `@ovellum/reader`    | 9      | frontmatter, zones (explicit + positional ids), anchor association, all error paths |
| `@ovellum/merger`    | 8      | splice, orphan quarantine, multi-block, anchorless warning                          |
| `@ovellum/site`      | 18     | markdown pipeline, nav tree, page template, landing renderer                        |
| **Total**            | **74** |                                                                                     |

Per the cadence rule, this count updates with each commit that adds or removes tests.
