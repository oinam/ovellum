---
'ovellum': minor
---

Simpler default-theme chrome: removed the editorial page frame — the two faint vertical rules along the content edges and the `+` corner marks where they met the header baseline and the footer's top border. The header now carries only its hairline border-bottom and the footer only its hairline border-top. The topbar, footer, and doc layout share one `--page-inset` horizontal step (replacing the internal `--frame-inset`/`--frame-gutter` pair), so the sidebar's left edge lines up with the topbar brand.
