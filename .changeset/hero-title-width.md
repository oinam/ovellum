---
'ovellum': patch
---

Widen the landing hero title so a longer tagline settles into two balanced lines. The hero `<h1>` cap was tuned for a short headline (16ch); it's now 30ch with `text-wrap: balance`, a better default for any hero copy.
