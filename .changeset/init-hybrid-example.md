---
'ovellum': patch
---

`ovellum init` now scaffolds a protected-zone example in hybrid mode. The starter `index.md` includes a `@manual` block — "Add your own notes here — this block survives every rebuild" — so new hybrid projects see the merge contract immediately. Manual-mode scaffolds are unchanged.
